<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrawerWide">

        <!-- File Provider for camera captures -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.drawer.platform.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths"/>
        </provider>

        <!-- Splash (Entry Point) -->
        <activity
            android:name=".onboarding.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.SplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Onboarding -->
        <activity android:name=".onboarding.OnboardingActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".onboarding.ModeSelectionActivity"
            android:screenOrientation="portrait"/>

        <!-- Auth -->
        <activity android:name=".auth.AuthActivity"
            android:screenOrientation="portrait"/>

        <!-- Seller -->
        <activity android:name=".seller.dashboard.SellerDashboardActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".seller.store.CreateStoreActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".seller.products.AddProductActivity"
            android:screenOrientation="portrait"/>

        <!-- Buyer -->
        <activity android:name=".buyer.home.BuyerHomeActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".buyer.reels.ReelsActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".buyer.product.ProductDetailActivity"
            android:screenOrientation="portrait"/>

        <!-- Deliver -->
        <activity android:name=".deliver.orders.DeliverOrdersActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".deliver.register.DeliverRegisterActivity"
            android:screenOrientation="portrait"/>

    </application>

</manifest>